{
  "name": "scrobbledb",
  "version": "2.0.0",
  "description": "Save your listening history from Last.fm or Libre.fm — Bun.js web app",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start":     "bun run src/index.ts",
    "dev":       "bun --hot run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
